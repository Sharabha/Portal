- if @problem_membership.errors.any?
  %div.alert-message.block-message.error
    %h4= t(:errors)
    - @problem_membership.errors.each do |attr, msg|
      %li
        = t(:field)
        %strong
          = t(attr)
        = msg
%br/
