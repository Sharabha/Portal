%h2= t(:invitations)
%br
- current_user.invitations.not_confirmed.each do |invitation|
  .p
    = invitation.team.name
    - if invitation.confirmed
      = t(:confirmed)
    - else
      = t(:not_confirmed)
      = link_to t(:confirm), "/confirm/#{invitation.token}"
