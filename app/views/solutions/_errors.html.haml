- if @solution.errors.any?
  %div.alert-message.block-message.error
    %h4= t(:errors)
    - @solution.errors.each do |attr, msg|
      %li
        = t(:field)
        %strong
          = t(attr)
        = msg
%br/
