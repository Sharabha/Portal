%h2 Sprawdzaczka dla zadania #{@checker.problem.name}
%br/
%br/
- if @checker.checker_datas.length > 0
  %h3 Dodane przykłady:
  - for cd in @checker.checker_datas
    .checker_data
      %b Dane wejściowe:
      = cd.input
      %br/
      %b Dane wyjściowe:
      = cd.output
      %br/
      %br/

= link_to "Dodaj nowe dane", new_problem_checker_checker_data_path(@problem)
